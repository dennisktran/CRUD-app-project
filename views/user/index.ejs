<!DOCTYPE html>
<html>
<head>
    <title>Account</title>
    <script src="https://kit.fontawesome.com/5af6d749d4.js"></script>
    <link rel="stylesheet" href="/css/style.css"/>
</head>
<body>
    <header>
        <div class="home-btn-div">
            <button class="home-btn"><a href="/"<i class="fas fa-home icon-btn"></a></i></button>
            <button class="ham-btn"><i class="fas fa-bars icon-btn"></i></button>
        </div>
        <div class="modal modal-account">
          <div class="modal-content">
                <div class="modal-header">
                    <a href="/user/logout" class="log-out">Logout</a>
                    <h1>Account</h1>
                </div>
                <div class="modal-body modal-account">
                    <form action="/user/<%=user._id%>?_method=PUT" method="POST" class="edit-user form">
                        <input type="text" name="name" value="<%=user.name%>"/><br/>
                        <input type="email" name="email" value="<%=user.email%>"/><br/>
                        <input type="password" name="password" placeholder="password"/><br/>
                        <input type="password" name="passwordcheck" placeholder="password check"/><br/>
                        <input type="text" name="pic" placeholder="pic" value="<%=user.pic%>"/><br/>
                        <button class="submit-btn btn">Edit</button>
                    </form>
                    <button class="edit-btn btn">Edit</button>
                    <form action="/user/<%=user._id%>?_method=DELETE" method="POST" class="form">
                        <button class=" delete-btn btn">Delete</button>
                    </form>
              </div>
          </div>
        </div>
        <h1><%=user.name%></h1>
    </header>
    <main>
        <section>
            <article class="center-art">
                <div class="user-vid">
                    <%if (likedVideo) {%>
                    <iframe src="https://youtube.com/embed/<%=likedVideo.url%>" class="main-vid"></iframe>
                    <div class="info-div">
                        <span><%=likedVideo.title%></span>
                        <span><i class="fas fa-thumbs-up"><%=likedVideo.likes.length%></i></span>
                        <span><i class="fas fa-thumbs-down"><%=likedVideo.dislikes.length%></i></span>
                    </div>
                    <div>
                        <ul>
                            <%for(let i = 0; i < likedVideo.comments; i ++){%>
                                <li><%=likedVideo[i].comments%></li>
                            <%}%>
                        </ul>
                    </div>
                    <%}%>
                </div>
            </article>
            <article>
                <button class="upload-btn">Upload</button>
                <div class="modal modal-upload">
                    <div class="modal-content">
                        <div class="modal-header">
                            <h1>Upload your Video!</h1>
                        </div>
                        <div class="modal-body">
                            <form action="/video/<%=user._id%>" method="POST" class="form">
                                <input type="text" name="user" value="<%=user.name%>" class="upload-creator"/>
                                <input type="text" name="url" placeholder="url"/>
                                <input type="text" name="title" placeholder="title"/>
                                <input type="text" name="tags" placeholder="tags"/>
                                <button class="submit-btn btn">Upload Video</button>
                             </form>
                        </div>
                    </div>
                </div>
                <h1><%=user.name%> video list</h1>
                <ul>
                    <%for(let i = 0; i < userVideos.videos.length; i++) {%>
                        <li class="user-vid-list">
                            <h1><%=userVideos.videos[i].title%></h1>
                            <form action="/video/<%=userVideos.videos[i]._id%>?_method=DELETE" method="POST">
                                <button>Delete</button>
                            </form>
                            <button class="edit-vid-btn">Edit</button>
                            <div class="modal modal-edit-vid">
                                <div class="modal-content">
                                    <div class="modal-header">
                                        <h1>Edit</h1>
                                    </div>
                                    <div class="modal-body">
                                        <form action="/video/<%=userVideos.videos[i]._id%>?_method=PUT" method="POST" class="form">
                                        <input type="text" name="title" value="<%=userVideos.videos[i].title%>"/>
                                        <input type="text" name="tags" value="<%=userVideos.videos[i].tags%>"/>
                                        <button>Update</button>
                                        </form>
                                    </div>
                                </div>
                            </div> 
                        </li>
                    <%}%>
                </ul>
            </article>
        </section>
    </main>
    <footer>
      <div class="socialmedia">
        <i class="fab fa-facebook-square"></i>
        <i class="fab fa-twitter-square"></i>
        <i class="fab fa-instagram"></i>
        <i class="fab fa-reddit-square"></i>
      </div>
    </footer>
<script src="/js/userIndex.js"></script>
</body>
</html>
